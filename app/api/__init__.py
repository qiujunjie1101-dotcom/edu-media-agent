"""api 子包：HTTP 边界层。

============================================================================
本层的三条纪律
============================================================================
1. **只做三件事**：解析/校验 HTTP 请求、调用服务、返回响应。
   不写业务规则、不做状态判断、不拼 SQL。
2. **绝不直接使用 Graph**：本层任何文件都不得 import LangGraph，
   图的所有操作都收口在 ``app.services.workflow_service.WorkflowService``。
   这样「LangGraph 升级要改哪些地方」这个问题，答案是唯一且明确的。
3. **服务实例靠依赖注入拿**：配置了哪些服务、装配了几份实例，
   由应用启动时的 lifespan 决定；本层只负责「取用」，不负责「创建」。

依赖方向是单向的：api → services → graph / adapters。
反向依赖（services 反过来 import api）是不允许的。
"""
