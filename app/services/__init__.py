"""services 子包：外部能力适配层 + 服务容器。

设计原则：

- 每个外部能力（文本模型、图片模型）都先定义**抽象接口**，再提供具体实现。
- S1 阶段只实现 Mock 版本；真实版本在 S7 / S8 阶段以「同签名的另一个类」加入，
  切换时只改配置文件与 ``container.py``，节点代码零改动。
- 本子包不得依赖 ``app.graph``，避免出现循环依赖。
"""
