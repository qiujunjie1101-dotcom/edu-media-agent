# ============================================================================
# 前端环境变量模板
# ============================================================================
# 使用方式：
#   cp .env.example .env
#
# 注意：只有以 VITE_ 开头的变量才会被 Vite 注入到浏览器代码里
# （这是防止把数据库口令之类的服务端密钥误打包进前端的保护机制）。

# 后端 API 基础地址。必须包含 /api/v1 前缀。
# 该地址的「来源」部分（协议 + 主机 + 端口）必须与后端 CORS_ALLOW_ORIGINS 登记的一致，
# 否则浏览器会拦截响应。默认后端监听 8000 端口。
VITE_API_BASE_URL=http://localhost:8000/api/v1
